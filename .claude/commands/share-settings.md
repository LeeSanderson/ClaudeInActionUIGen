Copy settings from settings.local.json to settings.json so they can be shared across the team.

Do the following:

1. If `settings.local.json` exists, merge the content with `settings.json` so this file can be checked into the repo and used across the team
2. Delete `settings.local.json`